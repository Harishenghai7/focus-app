// Export default Layout component (JS version)
export { default } from './Layout';
export { default as Layout } from './Layout';

// Export the hook (JS version)
export { useLayout } from './useLayout';
