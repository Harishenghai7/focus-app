/**
 * getAudioDuration
 * Extract audio length (mocked for demo).
 * @param {File} file
 * @returns {Promise<number>} duration in seconds
 * @example getAudioDuration(file)
 */
export default async function getAudioDuration(file) {
  // Replace with real duration logic
  return 30;
}
