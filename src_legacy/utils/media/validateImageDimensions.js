/**
 * validateImageDimensions
 * Check image size limits (mocked for demo).
 * @param {File} file
 * @param {number} maxWidth
 * @param {number} maxHeight
 * @returns {Promise<boolean>} valid
 * @example validateImageDimensions(file, 1000, 1000)
 */
export default async function validateImageDimensions(file, maxWidth, maxHeight) {
  // Replace with real validation logic
  return true;
}
