/**
 * generateBlurHash
 * Create blur placeholder hash (mocked for demo).
 * @param {File} file
 * @returns {Promise<string>} blurHash
 * @example generateBlurHash(file)
 */
export default async function generateBlurHash(file) {
  // Replace with real blurhash logic
  return 'blurhash';
}
