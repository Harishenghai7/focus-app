/**
 * detectFaces
 * Face detection in images (mocked for demo).
 * @param {File} file
 * @returns {Promise<Array<{x:number,y:number,width:number,height:number}>>} faces
 * @example detectFaces(file)
 */
export default async function detectFaces(file) {
  // Replace with real face detection
  return [];
}
