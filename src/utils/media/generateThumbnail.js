/**
 * generateThumbnail
 * Create image thumbnails (mocked for demo).
 * @param {File} file
 * @returns {Promise<string>} thumbnailUrl
 * @example generateThumbnail(file)
 */
export default async function generateThumbnail(file) {
  // Replace with real thumbnail logic
  return 'thumbnail-url';
}
