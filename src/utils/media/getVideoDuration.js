/**
 * getVideoDuration
 * Extract video length (mocked for demo).
 * @param {File} file
 * @returns {Promise<number>} duration in seconds
 * @example getVideoDuration(file)
 */
export default async function getVideoDuration(file) {
  // Replace with real duration logic
  return 60;
}
