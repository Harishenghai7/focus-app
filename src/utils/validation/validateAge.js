/**
 * validateAge
 * Age verification (must be >= 13).
 * @param {number} age
 * @returns {boolean} valid
 * @example validateAge(18)
 */
export default function validateAge(age) {
  return age >= 13;
}
